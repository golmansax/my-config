#!/bin/bash

usage() {
  USAGE='Usage: github-clone [repo-account] [repo] '
  USAGE+='[optional: output-folder]'
  echo $USAGE
  exit
}

[[ $# -lt 2 ]] && usage

git clone ssh://git@github.com/$1/${2}.git $3
